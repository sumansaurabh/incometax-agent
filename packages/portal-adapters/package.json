{
  "name": "@itx/portal-adapters",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo 'portal-adapters build complete'",
    "lint": "echo 'lint placeholder for portal-adapters'",
    "test": "tsx --test tests/**/*.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "tsx": "^4.20.0"
  }
}
